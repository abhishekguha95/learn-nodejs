Counter with Socket.io
This is a simple Node.js + Express + Socket.io application that demonstrates real-time communication using WebSockets.

The app consists of:

Backend: Node.js server using Express and Socket.io
Frontend: A simple HTML client that connects to the WebSocket server
The app shows a counter value that increments every second on the server, and clients can listen to the counter updates in real-time using WebSocket connections.